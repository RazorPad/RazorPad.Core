<UserControl x:Class="RazorPad.Views.Sidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0">Model Provider:</Label>
        <ComboBox Grid.Row="1" ItemsSource="{Binding AvailableModelProviders}" SelectedValue="{Binding SelectedModelProvider}" />

        <ContentPresenter Grid.Row="2" Content="{Binding ModelBuilder}" />
    </Grid>

</UserControl>
